# vi: ft=dockerfile

FROM base/archlinux
EXPOSE 80/tcp
RUN useradd -m -G wheel -s /bin/bash monkey
RUN pacman-db-upgrade
RUN pacman -Syyu --noconfirm && pacman --noconfirm -S nginx
WORKDIR /usr/share/nginx/html
CMD ["nginx", "-g", "daemon off;"]
